{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "assignees": ["cascer1"],
    "rebaseWhen": "conflicted",
    "customManagers": [
      {
        "customType": "regex",
        "managerFilePatterns": ["cargo.yaml"],
        "matchStrings": [
          "^(?<depName>\\w+)\\s*:\\s*(?<currentValue>\\d+\\.\\d+\\.\\d+)"
        ],
        "depNameTemplate": "cargo crate ${depName}",
        "datasourceTemplate": "crate"
    }
  ]
}

